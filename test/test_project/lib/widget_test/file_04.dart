import 'package:flutter/material.dart';

class File04 extends StatelessWidget {
  const File04({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          width: 20,
          height: 20,
          color: Colors.red,
        ),
      ),
    );
  }
}
