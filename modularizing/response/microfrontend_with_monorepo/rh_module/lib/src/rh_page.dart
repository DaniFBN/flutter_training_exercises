import 'package:flutter/material.dart';

class RhPage extends StatelessWidget {
  const RhPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rh Page'),
      ),
      body: const Center(child: Text('Rh Page')),
    );
  }
}
