import 'package:flutter/material.dart';

class File01 extends StatelessWidget {
  const File01({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Detect this Text')),
    );
  }
}
