import 'package:flutter/material.dart';

class RhDetailPage extends StatelessWidget {
  const RhDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rh Detail Page'),
      ),
      body: const Center(child: Text('Rh Detail Page')),
    );
  }
}
